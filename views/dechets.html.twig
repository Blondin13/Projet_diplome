{% extends "./index.html.twig" %}
{% block header %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
        <div class="container">
            <a class="nav-logo navbar-brand" href="#">
                <img src="/assets/img/logo2.png" alt="logo.eco" width="60" height="60" />
            </a>
            <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation"
            >
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="div-navbar collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="text-end navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" href="/accueil">Retour a l'accueil</a>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right text-end">
                  <li><a class="btn-connexion" href="/deconnexion"><span class="bi bi-person-fill"></span> Déconnexion</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block main %}
<main class="main-dechets container">
        <aside>
        <p class="p-2">Informations</p>
            <div>
                <a href="/monprofil/{{user.id}}">Coordonnées</a>
            </div>
            <div>
                <a href="/besoins/{{user.id}}">Besoins</a>
            </div>
            <div>
                <a href="/productions/{{user.id}}">Productions</a>
            </div>
            <div>
                <a class="active" href="/dechets/{{user.id}}">Déchets</a>
            </div>
        </aside>
	
		<article class="article-dechets">
             <form class="form-dechets" id="dechets" action="/dechets" method="post">
             <img class="paper-attention-b" src="/assets/img/papier.png">
                <p class= "alerte-info"><strong>*Attention : Au moins un des champs "Besoins, Productions ou Déchets" est obligatoire*</strong></p>
                    <img class="p-gauche-b" src="/assets/img/p-gauche.png">
                    <img class="p-droite-b" src="/assets/img/p-droite.png">
             
                <fieldset>
                    <label for="text"><b class="d-monprofil"> Vos déchets </b></label>
                    <textarea class="textarea-dechets" name="dechets" id="text" cols="30" rows="10" placeholder="..........">{{user.dechets}}</textarea>
                        <div class="div-2-btn">
                            <button class="btn-besoins" formaction="/dechets/{{user.id}}/{{step}}"><b>Valider</b></button>
                        </div>
                </fieldset>
                    <div class = "error">
                        {% for error in errors %}
                            <span class="erreur">{{error}}</span>
                        {% endfor %}
                    </div> 
            </form>
        </article>

</main>
{% endblock %}
{% block footer %}
{% endblock %}